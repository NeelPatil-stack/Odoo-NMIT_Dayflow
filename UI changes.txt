Remove the existing **Monthly Attendance Trend** chart completely from the Admin Dashboard.

The chart is repeatedly showing incorrect raw dates, zero values, and an empty visualization even after multiple fixes.

Do NOT spend more time trying to repair this chart.

Replace the entire section with a more useful and visually polished:

# Today's Attendance Summary

Use ONLY the existing dashboard attendance data.

Do not change backend logic, APIs, database records, or attendance calculations.

---

## New Layout

Create a premium attendance summary panel.

### Left Side

Display:

**Today's Attendance**

Supporting text:

**A quick overview of today's workforce attendance.**

Then show four compact metrics:

* Total Employees
* Present
* Absent
* On Leave

Use the exact values already available on the dashboard.

Do NOT hardcode values.

---

## Attendance Progress Visualization

Below the metrics, create clean horizontal progress bars.

Example visual structure:

**Present**

`████████████████████ 92%`

**Absent**

`██ 4.5%`

**On Leave**

`██ 3.5%`

Calculate percentages using the existing dashboard values.

Do not modify the underlying attendance records.

---

## Colors

Present:

Green

`#22A06B`

Absent:

Red

`#E5484D`

Leave:

Orange

`#F59A23`

Remaining/background progress track:

`#EAF0F6`

Keep colors soft and professional.

---

## Right Side

Create a prominent Attendance Rate indicator.

Example:

**Attendance Rate**

**92%**

Use a circular progress ring or radial indicator.

The percentage should be calculated using:

`Present / Total Employees × 100`

Use the existing values.

Do not hardcode 92%.

---

## Circular Progress

Create a clean circular progress indicator.

Center:

Large percentage.

Below:

**Present Today**

Use Royal Blue / Green.

Keep it minimal.

Do not use a pie chart with many slices.

---

## Optional Secondary Information

If the existing data is available, show small details such as:

* Present Today
* Absent Today
* On Leave
* Pending Regularization

Do not invent additional metrics.

---

## Remove Old Chart Code

Search for the exact component rendering:

**Monthly Attendance Trend**

Remove:

* LineChart
* Line
* BarChart
* Bar
* raw date X-axis
* Last 6 Months control
* monthly trend tooltip

from this specific section if they are no longer used.

The old chart must not remain hidden in the component.

---

## New Section Structure

Recommended desktop layout:

```text
┌─────────────────────────────────────────────────────────────┐
│ Today's Attendance                                         │
│ A quick overview of today's workforce attendance.          │
│                                                             │
│ Present          ███████████████████        92%             │
│ Absent           ██                         4.5%            │
│ On Leave         ██                         3.5%            │
│                                                             │
│ Total: 200                                               ◯  │
│                                                       92%   │
│                                                Attendance   │
└─────────────────────────────────────────────────────────────┘
```

Use proper responsive layout rather than copying this ASCII structure literally.

---

## Animation

When the section loads:

Progress bars should animate from:

`width: 0`

to their actual percentage.

Duration:

approximately `500–700ms`.

Circular progress should animate once.

Do not loop the animation.

---

## Styling

Use the existing KaaryaSetu design language:

* white card
* soft border
* subtle shadow
* 18–20px radius
* good whitespace
* navy headings
* soft tinted progress tracks

Avoid making it look like another generic KPI card.

---

## Responsive Behavior

Desktop:

Attendance bars on the left.

Attendance Rate indicator on the right.

Mobile:

Stack vertically.

No overlapping or horizontal overflow.

---

## Important

Do NOT change:

* Total Employees value
* Present value
* Absent value
* Leave value
* dashboard API
* attendance API
* backend
* database
* other charts
* Department Distribution

Only replace the broken **Monthly Attendance Trend** visualization with a reliable **Today's Attendance Summary** component using existing dashboard data.

The final dashboard should no longer display raw dates such as:

`2026-08-18`

or an empty 0–4 attendance graph.
